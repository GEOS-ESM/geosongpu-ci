# Discover on demand

name: discover-ondemand

on:
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  run-discover-ondemand:
    uses: GEOS-ESM/CI-workflows/.github/workflows/geosongpu-ondemand.yml@516b6cdf5209981a4d1c25becb03c94ad890127d